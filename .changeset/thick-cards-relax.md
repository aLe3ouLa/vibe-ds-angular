---
"@alexandra/design-system": minor
---

Add `ds-card`, a content container with `variant` (`default`/`subtle`) and
`padding` (`sm`/`md`/`lg`). See RFC 002 for what's deferred (elevation,
header/footer slots, clickable cards).
